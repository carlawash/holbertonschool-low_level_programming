#include "holberton.h"
#include <stdio.h>

/**
 * abs - returns absolute number
 * @n: prints number
 * Return:(0)
 */

int _abs(int n)
{

	if (n > 0)
	{
		return (n);
	}
	if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	return (0);
}
